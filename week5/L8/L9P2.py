# Write the code that adds the appropriate edges to the graph
# in this box.

g.addEdge(Edge(nodes[abc],nodes[acb]))
g.addEdge(Edge(nodes[abc],nodes[bac]))
g.addEdge(Edge(nodes[acb],nodes[cab]))
g.addEdge(Edge(nodes[bac],nodes[bca]))
g.addEdge(Edge(nodes[bca],nodes[cba]))
g.addEdge(Edge(nodes[cba],nodes[cab]))